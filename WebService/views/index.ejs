<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>VAG Stats</title>
    <style>
      body {
        font-family: sans-serif;
        margin: 0;
        padding: 20px;
        background: #f9f9f9;
      }
      .grid-container {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 20px;
      }
      .grid-item {
        background: #fff;
        border: 1px solid #ddd;
        padding: 10px;
        text-align: center;
        box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.1);
      }
      .grid-item img {
        max-width: 100%;
        height: auto;
        display: block;
        margin: 0 auto 10px;
      }
      .grid-item p {
        margin: 0 0 10px;
      }
      .grid-item a {
        color: #007acc;
        text-decoration: none;
      }
      footer {
        position: fixed;
        z-index: 1000;
        bottom: 0;
        left: 0;
        width: 100%;
        background: #333;
        color: #fff;
        text-align: center;
        height: 30px;
        font-size: 0.8em;
      }
      footer a {
        color: #fff;
      }
    </style>
  </head>
  <body>
    <div class="grid-container" id="gridContainer">
      <!-- Grid items will be inserted here -->
    </div>
    <footer>
      <p><%= appName %>@<%= commitId %> <a href="https://github.com/Public-Transport-Nuremberg/VAGStatsTracker">Source</a> <a href="/legal/legal">Imprint</a></p>
    </footer>

    <script>
      // Define an array of objects for each grid item
      const gridItems = [
        {
          image: "/dist/img/318438398-1b42237a-1d7c-4da9-9092-963dad3c6972.webp",
          description: "Livekarte mit dem aktuellen Standort der Fahrzeuge der VAG Nürnberg. Die Fahrzeuge können angeklickt werden, um weitere Informationen zu erhalten.",
          url: "/livemap",
        },
        {
          image: "/dist/img/318444039-a6953e61-28fb-4892-83ab-2da07ac3c001.webp",
          description: "Heatmap von Verspätungen der Fahrzeuge der VAG Nürnberg. Die Heatmap zeigt die durchschnittliche Verspätungen der Fahrzeuge an Stationen eines bestimmten Tages (Standard gestern).",
          url: "/heatmap",
        },
        {
          image: "/dist/img/linestats.webp",
          description: "Tabelle mit der durchschnittlichen Ankunfts-/Abfahrtsverzögerung in Sekunden pro Station und Stunde. Die Linien werden aus den Daten extrahiert und spiegeln möglicherweise keine vorübergehenden Änderungen wider.",
          url: "/linestats",
        },
        {
          image: "/dist/img/vehicleHistory.webp",
          description: "Schau dir die Geschichte eines Fahrzeugs an. Klicke herzu auf eine den Marker eines Fahrzeugs und dann auf „Fahrzeughistorie“.",
          url: "/livemap",
        },
        {
          image: "/dist/img/ProductLineChart.webp",
          description: "Verspätungstatistik pro Woche oder Monat. Die Daten werden in einem Liniendiagramm dargestellt.",
          url: "/ontimelinechart",
        }
      ];

      // Get the grid container element
      const gridContainer = document.getElementById("gridContainer");

      // Loop through each item in the array and create the grid elements
      gridItems.forEach((item) => {
        // Create a container div for the grid item
        const gridItem = document.createElement("div");
        gridItem.className = "grid-item";

        // Create and set the image element
        const img = document.createElement("img");
        img.src = item.image;
        img.alt = item.description;

        // Create a paragraph for the description
        const desc = document.createElement("p");
        desc.textContent = item.description;

        // Create a link element
        const link = document.createElement("a");
        link.href = item.url;
        link.textContent = "Probiere es aus";

        // Append the image, description, and link to the grid item
        gridItem.appendChild(img);
        gridItem.appendChild(desc);
        gridItem.appendChild(link);

        // Append the grid item to the container
        gridContainer.appendChild(gridItem);
      });
    </script>
  </body>
</html>
