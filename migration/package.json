{
  "name": "clickhouse-migrations",
  "version": "1.0.0",
  "description": "ClickHouse schema migrations",
  "main": "migrate.js",
  "scripts": {
    "migrate": "node migrate.js",
    "migrate:clickhouse": "node migrate.js"
  },
  "dependencies": {
    "@clickhouse/client": "^1.0.0",
    "dotenv": "^16.0.0"
  }
}

